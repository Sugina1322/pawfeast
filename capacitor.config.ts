import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pawfeast.app',   // Update this to your chosen package name
  appName: 'pawfeast',
  webDir: 'www'
};

export default config;
